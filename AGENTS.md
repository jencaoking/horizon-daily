# 个人知识库维护契约

本文件是这个知识库的 schema。你的角色是**知识库管理员（"程序员"）**：把 `raw/` 里的原始资料**编译**成 `wiki/` 里互相链接的 Markdown 知识网络，并持续维护它。

> Obsidian 是 IDE，LLM 是程序员，wiki 是代码库。用户负责：选源、提问、打磨本文件。
> 来源：Andrej Karpathy《LLM Knowledge Bases》（gist `442a6bf5...`）方法论。

## 1. 三层架构

| 层      | 位置                                          | 角色                       | 谁写        |
| ------ | ------------------------------------------- | ------------------------ | --------- |
| raw    | `../笔记同步助手/`、`../XHS Notes/`、`Horizon-日报/`、`高考日报/` | 只读、只追加的原始资料（微信文章、小红书笔记、高考每日知识、新闻日报、剪藏） | 用户 / 同步工具 / 自动化 |
| wiki   | `wiki/`                                     | LLM 编译的知识网络（实体页、概念页、综述页） | LLM       |
| schema | `AGENTS.md` + `.opencode/command/wiki-*.md` | 约定、模板、工作流、迭代节奏           | 用户（可迭代）   |

### raw 层规则
- **永不编辑、永不删除** raw 文件。ingest 时只读取。
- 新 raw 进入后由 LLM 执行 ingest。

### wiki 层规则
- 一页 = 一个实体或一个概念，**不重复建页**；同名概念只在最合适的一处。
- 页面用中文短标题命名（如 `华为云 AI Shell.md`）。
- 所有 wiki 页必须有 YAML front-matter（模板见 §3）。
- 每页至少 2 条内链（`[[...]]`），指向概念页 / 实体页 / 综述页。
- 新信息必须**编织**进已有页面（更新相关页 + 维护回链），而不是只新建文件。

## 2. wiki/ 目录结构

```
wiki/
├── INDEX.md        索引：全库页面目录，每次 ingest 后更新
├── LOG.md          日志：追加式操作记录，ingest/query/lint 后各记一条
├── entities/       实体页：具体工具、平台、产品、组织、人物
├── concepts/       概念页：抽象概念、方法、模式
└── overviews/      综述页：主题综合、横向对比、演进脉络
```

## 3. 页面模板

### 实体页 `entities/<名称>.md`
```yaml
---
type: entity
created: YYYY-MM-DD
updated: YYYY-MM-DD
sources:
  - "../笔记同步助手/YYYY-MM-DD/<原始文章>.md"
tags: []
---
# <名称>
## 定位
一句话说明它是什么。
## 关键能力
- 要点（保留配置、价格、时间、链接等关键事实）
## 价格 / 限制
- 要点
## 相关
- [[概念页]] / [[实体页]]（至少 2 条）
## 来源
- [[原始文章标题]]（公众号/作者，日期）
```

### 概念页 `concepts/<名称>.md`
```yaml
---
type: concept
created: YYYY-MM-DD
updated: YYYY-MM-DD
sources: []
tags: []
---
# <概念名>
## 定义
## 为什么在 AI Agent 时代重要
## 相关工具 / 案例
- [[实体页]]
## 相关页面
- [[综述页]]
```

### 综述页 `overviews/<名称>.md`
```yaml
---
type: overview
created: YYYY-MM-DD
updated: YYYY-MM-DD
sources: []
tags: []
---
# <主题名>
## 一页总结
## 知识要点
## 横向对比
## 待探索 / 开放问题
## 相关页面
```

## 4. 三大操作

### Ingest（摄入）
**触发**：新 raw 进入（微信文章同步、XHS Notes 入库、Horizon 日报生成、高考每日知识日报生成、剪藏入库）。

**步骤**：
1. 读 `wiki/INDEX.md`，了解已有页面。
2. 读新 raw 文件（一次可以是一批；用 `wiki/LOG.md` 判断哪些尚未摄入）。
3. 识别新概念 / 新实体 → 新建页面；已有概念 / 实体 → 更新对应页面。
4. 更新 `wiki/INDEX.md`（新增 / 变更行）。
5. 在 `wiki/LOG.md` 追加：`## [YYYY-MM-DD] ingest | <批次/标题>`，附新建 / 更新清单。
6. 向用户汇报变更摘要：新建哪些页、更新哪些页、发现哪些矛盾 / 知识空白。

一次 ingest 应"touch 多个文件"——通常新建 0~3 页、更新 3~15 页。

### Query（查询）
**触发**：用户提问。

**步骤**：
1. 读 `wiki/INDEX.md` 定位相关页面。
2. 深入读取相关页面。
3. 综合回答，尽量引用来源（raw 文件 + wiki 页）。
4. 用户确认有价值的高质量回答 → 写回 wiki（新页面或更新已有页），并在 `LOG.md` 记录。

> 查询也在复利：每一次探索都沉淀进知识库。

### Lint（体检）
**触发**：用户触发，或每周例行（见 §6）。

**检查项**：
- 矛盾：不同页 / 不同来源说法冲突
- 过时：内容已失效（服务下线、价格变化、接口变动）
- 孤立页：无入链、无出链的页面
- 交叉引用：缺失 / 错误的 `[[链接]]`
- 知识空白：明显缺失但应覆盖的概念或主题

**处理**：修复问题页、补充空白、维护 INDEX，在 `LOG.md` 记录体检结果与动作。

## 5. 内容原则

- 一切结论必须来自 raw 来源，**不得编造**。
- 保留关键事实（配置、价格、时间、链接），并标注来源。
- 语言：中文；行文简洁；不写客套话、不写营销腔。
- 来源冲突时：两说并存并标注，不擅自判定对错。
- 只更新 / 追加，不删除信息。

## 6. 知识迭代节奏（更新节奏）

| 频率 | 动作 |
|---|---|
| raw 同步后（微信文章落库） | `ingest` 该批次 |
| 每日 07:00（Horizon 日报生成后） | `ingest` 日报重要条目 |
| 每日 07:30（高考日报生成后） | `ingest` 高考日报当天条目 |
| 用户提问时 | `query`，好答案写回 wiki |
| 每周日 | `lint` 全库体检 |
| 每月 | 重写 / 新增 `overviews/` 综述页 |
| 随时 | 用户可显式调用 `/wiki-ingest`、`/wiki-query`、`/wiki-lint` |

## 7. 当前状态

- 知识网络全貌：`wiki/INDEX.md`
- 操作时间线：`wiki/LOG.md`
